- name: "script"
  tasks:
  - shell: "true"

- name: "after_success"
  environment:
    BUNDLE_GEMFILE: "${PWD}/Gemfile"
    GEM_HOME:       "~/.rubygems"
    RAILS_ENV:      test
    RACK_ENV:       test

  tasks:
  - shell: sh -c "echo 1 ; sleep 1 ; echo 1 > log.txt" &
  - shell: sleep 2
