- name: "script"
  tasks:
  - shell: "true"

- name: "after_success"
  environment:
    BUNDLE_GEMFILE: "${PWD}/Gemfile"
    GEM_HOME:       "~/.rubygems"
    RAILS_ENV:      test
    RACK_ENV:       test

  tasks:
  - shell: sudo -u dima psql -h localhost -U postgres -d template1 -c "CREATE EXTENSION postgis;"
  - shell: echo ${PWD} ${HOME}
  - shell: "false"
  - shell: sh -c "ls -la"
